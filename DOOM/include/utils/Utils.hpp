#ifndef __DOOM__UTILS__
#define __DOOM__UTILS__

// A tag to specify that a variable is not used in the code, while it is
// necessary it exists. Used to suppress warnings/errors about such variables.
#define UNUSED(x) (void)x

#endif
